[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    network: test downloads a pretrained checkpoint or tokenizer from the Hub.
        Deselect with `-m "not network"` when running offline.
